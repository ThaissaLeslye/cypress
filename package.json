{
  "name": "cypress-playground",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cypress": "^14.5.4",
    "mocha-junit-reporter": "^2.2.1"
  }
}
